/**
 * Tap Rush — Frontend Config
 *
 * Re-exports shared config for the frontend.
 */

export {
  TAP_RUSH_CONFIG,
  calculateComboMultiplier,
  calculateTapScore,
  generateTargetPosition,
  isTapOnTarget,
  getNextSpawnDelay,
  shouldSpawnBonusTarget,
} from './types';
